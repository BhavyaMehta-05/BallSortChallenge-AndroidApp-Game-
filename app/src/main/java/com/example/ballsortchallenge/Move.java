package com.example.ballsortchallenge;

public class Move {
    int fromTube;
    int toTube;
    int color;

    public Move(int fromTube, int toTube, int color) {
        this.fromTube = fromTube;
        this.toTube = toTube;
        this.color = color;
    }
}
